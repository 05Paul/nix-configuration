-- general
hl.config({
    general = {
        gaps_out = 5,
    },
})

-- decoration
hl.config({
    decoration = {
        rounding = 10,
        inactive_opacity = 0.9,
    },
})

-- debug
hl.config({
    debug = {
        disable_logs = false,
    },
})

-- group
hl.config({
    group = {
        groupbar = {
            font_size = 18,
            height = 20,
            gradients = true,
            gradient_rounding = 6,
            indicator_height = 0,
        },
    },
})
